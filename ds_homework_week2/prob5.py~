def Square_root():
    c = int(input("c = "))
    g = c/int(input("factor of g = "))
    i = 0
    while(abs(g*g -c) > 0.00000000001):
        g = (g + c/g)/2
        i += 1
        print("%d:%.13f"%(i,g))
        
Square_root()
